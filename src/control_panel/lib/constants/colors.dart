import 'package:flutter/material.dart';

/// Defines the colors used in the project
class ProjectColors {
  static Color primary = const Color(0xFF0958EF);
  static Color? greyLight = Colors.grey[400];
  static Color? greyDark = Colors.grey[700];
  static Color greenValidate = const Color.fromARGB(255, 105, 203, 109);
  static Color redError = const Color.fromARGB(255, 255, 101, 90);
  static Color white = Colors.white;
}
